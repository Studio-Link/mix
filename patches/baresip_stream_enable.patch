diff --git a/src/stream.c b/src/stream.c
index 104259a3..5d2f9c92 100644
--- a/src/stream.c
+++ b/src/stream.c
@@ -354,7 +354,7 @@ static void rtp_handler(const struct sa *src, const struct rtp_header *hdr,
 
 	MAGIC_CHECK(s);
 
-	if (!s->rx.enabled && s->type == MEDIA_AUDIO)
+	if (!s->rx.enabled)
 		return;
 
 	if (rtp_pt_is_rtcp(hdr->pt)) {
